---
import AdminLayout from '../../../layouts/admin.astro';
import type { PlaceType } from '../../../shared/types';

export const prerender = false;
const response = await fetch(`${import.meta.env.PUBLIC_API_URL}/api/admin/placetypes`);
const types: PlaceType[] = await response.json();
---

<AdminLayout title="Manage Types">
  <div class="text-sm breadcrumbs">
    <ul>
      <li><a href="/manage">Admin</a></li>
      <li>Types</li>
    </ul>
  </div>
  <div class="flex justify-between items-center mb-4">
    <h1 class="text-2xl font-bold">Types</h1>
    <a href="/manage/types/add" class="btn btn-warning">Add Type</a>
  </div>
  <table class="table w-full max-w-xl">
    <thead>
      <tr>
        <th>Name</th>
        <th>Active</th>
        <th></th>
      </tr>
    </thead>
    <tbody>
      {types.map((t) => (
        <tr>
          <td>{t.name}</td>
          <td>{t.active ? 'Yes' : 'No'}</td>
          <td><a href={`/manage/types/${t.id}`} class="btn btn-sm btn-ghost">Edit</a></td>
        </tr>
      ))}
    </tbody>
  </table>
</AdminLayout>
